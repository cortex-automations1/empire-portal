{
  "name": "@empire/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@empire/eslint-config": "workspace:*",
    "@empire/typescript-config": "workspace:*",
    "typescript": "^5.3.3",
    "vitest": "^4.0.18"
  }
}
